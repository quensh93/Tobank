# flows/promissory_real/api/GET_asset.json

Source: lib/stac/tobank/flows/promissory_real/api/GET_asset.json

## JSON Paths (sample)
- Could not parse JSON structure for path extraction.

## Raw JSON
```json
{
    "GET": {
        "statusCode": 200,
        "data": {
            "sign_coordination": {
                "x": 120,
                "y": 450,
                "width": 180,
                "height": 60,
                "page": 1
            }
        }
    }
}
```
