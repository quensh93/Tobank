import 'package:flutter/material.dart';

class PhysicalGiftCardStatusData {
  String? status;
  String? statusTitle;
  Color? statusColor;

  PhysicalGiftCardStatusData({
    this.status,
    this.statusTitle,
    this.statusColor,
  });
}
