- 🔧 **refactor**: Eliminate custom deposits parser (`promissory_real_deposits_parser.dart`, 622 lines) and replace with generic `ReactiveListViewParser`
- ✨ **feat**: Create reusable `ReactiveListViewParser` for reactive data lists with selection state support
- 🔧 **refactor**: Rewrite `promissory_real_deposits_screen.dart` using `conditional` and `reactiveListView` Stac widget types
- 🔧 **refactor**: Update `register_custom_parsers.dart` to register `ReactiveListViewParser` instead of deposits-specific parser
- 📝 **docs**: Create full documentation for `ReactiveListViewParser` at `docs/App_Docs/stac_docs/widgets_reactive_listview.md`
- 🐛 **fix**: Fix item selection not updating on tap — `StatefulWidgetParser` was resolving `{{item.*}}` placeholders before `ReactiveListViewParser` could use them
- 🔧 **refactor**: Modify `StatefulWidgetParser._resolveExpressionsInJson()` to skip protected keys inside `reactiveListView` widgets
- 🔧 **refactor**: Rewrite `ReactiveListViewParser` to use Flutter's native `GestureDetector` + `Stac.onCallFromJson()` for tap handling
- 🔧 **refactor**: Eliminate custom deposits parser (`promissory_real_deposits_parser.dart`, 622 lines) and replace with generic `ReactiveListViewParser`
- ✨ **feat**: Create reusable `ReactiveListViewParser` for reactive data lists with selection state support
- 🔧 **refactor**: Rewrite `promissory_real_deposits_screen.dart` using `conditional` and `reactiveListView` Stac widget types
- 🔧 **refactor**: Update `register_custom_parsers.dart` to register `ReactiveListViewParser` instead of deposits-specific parser
- 📝 **docs**: Create full documentation for `ReactiveListViewParser` at `docs/App_Docs/stac_docs/widgets_reactive_listview.md`
- 🐛 **fix**: Fix item selection not updating on tap — `StatefulWidgetParser` was resolving `{{item.*}}` placeholders before `ReactiveListViewParser` could use them
- 🔧 **refactor**: Modify `StatefulWidgetParser._resolveExpressionsInJson()` to skip protected keys inside `reactiveListView` widgets
- 🔧 **refactor**: Rewrite `ReactiveListViewParser` to use Flutter's native `GestureDetector` + `Stac.onCallFromJson()` for tap handling

-  **feat**: Created promissory_real_deposits_screen_clone.dart using strongly-typed Stac models
-  **docs**: Updated widgets_reactive_listview.md with comprehensive usage, generic API support, and reactivity details

-  **refactor**: Replace clone deposits screen with original name, delete old raw-JSON version

-  **refactor**: Rewrite promissory_real_payment_deposits_screen.dart with typed Stac widgets, preserving draft API call
- 🐛 **fix**: Fix stale deposit selection in payment deposits screen — onInit was not clearing `selectedDeposit.*` keys from previous screen
