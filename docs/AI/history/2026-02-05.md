- 🐛 **fix**: Fixed deposit screen data loading issue - `deposits.rawData` was receiving stale identity verification data instead of actual deposit list
- 🔍 **analysis**: Identified root cause - STAC framework pre-processes `{{data_payload}}` templates at JSON parse time, not execution time, causing stale cached values
- ✨ **feat**: Added `_shouldFetchFreshFromRegistry()` method in `CustomSetValueActionParser` to detect and bypass stale pre-resolved values
- 🔄 **fix**: Implemented intelligent stale data detection by checking for identity-related fields (`birthDate`, `nationalId`, `fatherName`) in pre-resolved values
- ✨ **feat**: Created new `log_config.dart` file for centralized log configuration with code-based overrides
- ✨ **feat**: Added granular STAC-specific log categories: `stacNavigation`, `stacWidget`, `stacRegistry`, `stacAction`, `stacTheme`, `stacMock`, `stacVariable`
- ⚡ **perf**: Disabled noisy STAC log categories by default to reduce console spam during development
- 📝 **feat**: Added single-line cURL command logging for network requests (copy-pasteable format)
- 📝 **feat**: Added single-line response logging with status code and truncated body preview
- ♻️ **refactor**: Updated `CustomNavigateActionParser` to use `LogCategory.stacNavigation` for granular control
- ♻️ **refactor**: Updated `CustomImageParser` to use `LogCategory.stacWidget` for granular control
- ♻️ **refactor**: Updated `StatefulWidgetParser` to use `LogCategory.stacRegistry` for granular control
- ♻️ **refactor**: Updated `StacWidgetResolver` to use `LogCategory.stacNavigation` and `stacTheme` for granular control
- ♻️ **refactor**: Moved `LogOverrides` class from `log_category.dart` to dedicated `log_config.dart` file
- 🔧 **fix**: Updated `logger.dart` to integrate `LogConfig` override system with proper hierarchy (master > category override > debug panel)
- ✨ **feat**: Added `_resolveActionTemplates()` method in `CustomNetworkRequestActionParser` for template pre-resolution
- 📝 **debug**: Added detailed logging for `data_payload` resolution to trace stale data issues
- 🧹 **cleanup**: Removed old promissory flow JSON screens (replaced with promissory flow)
- 🔧 **fix**: Updated `promissory_payment_deposits.json` to use `{{data_payload}}` for deposit data binding
