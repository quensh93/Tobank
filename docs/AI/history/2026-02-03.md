- 🐛 **fix**: Resolved `widgetType` parameter error in `promissory_login_screen.dart`
- ♻️ **refactor**: Cleaned up `register_custom_parsers.dart` and fixed corrupted function definition
- 🏗️ **build**: Verified project build with `build_runner`
- ♻️ **refactor**: Refactored `promissory` flow screens (sign, success, deposits, settings) to single-function structure
- ✨ **feat**: Added error handling (retry button) for network requests in promissory deposits screens
- 🐛 **fix**: Replaced invalid `statusCode: 'default'` with `-1` and updated parser to support generic error fallback
- ⚡ **perf**: Enforced 15-second timeout on custom network requests to prevent infinite loading
- ✨ **feat**: Implemented loading state and retry logic in `promissory_issuer_screen` to hide raw template strings
- ✨ **feat**: Created `PromissoryRealIssuerParser` custom parser for proper loading/error/success state management
- ✨ **feat**: Added `loadingKey` support to `ReactiveElevatedButtonParser` for showing loading state on buttons
- ✨ **feat**: Created `ShowSnackBarActionParser` for showing error notifications via snackbar
- 🐛 **fix**: Added 422 status code handling in receiver screen to show "رکوردی یافت نشد" error
- ✨ **feat**: Added loading state and error handling for continue button in receiver screen
- 🐛 **fix**: Fixed sign screen button enabled state (changed `enabledKey: 'true'` to `enabled: true`)
- ✨ **feat**: Initialized default values for signPage/signX/signY in sign screen for proper template binding
- 📝 **docs**: Added signConfirmationTitle and signConfirmationMessage to GET_strings.json
- ♻️ **refactor**: Replaced all hardcoded Persian strings with appStrings references in promissory flow
- 📝 **docs**: Added 25+ new strings to GET_strings.json for promissory flow localization
- 🛠️ **fix**: Fixed `dialog` and `toast` action types in `promissory` flow
- 🚀 **feat**: Added mock API files and interceptor support for `promissory` flow
- 🔧 **refactor**: Finalized localization and error handling in `promissory_sign_screen`
- 🐞 **fix**: Added workaround for missing `promissory_request_id` in signature process
- 🛠️ **fix**: Replaced `customSetValue` with `setValue` in `promissory_sign_screen` to fix "Action type not supported" error
- 🎨 **style**: Updated `promissory_success_screen` UI to match reference design (icons, spacing, typography)
- 📝 **docs**: Refactored `promissory_success.dart` (static flow) to use localized `appStrings` instead of hardcoded strings
- 🐛 **fix**: Corrected `transactionType` value in static promissory success screen to use "Issuance" title instead of label
- 🔧 **fix**: Standardized text binding in `promissory_success_screen` and added fallback values in `promissory_sign` to prevent missing data in UI
- 🛠️ **fix**: Created `GET_deposits.json` mock and updated `stac_mock_dio_setup.dart` to fix interceptor error for `promissory_payment_deposits`
- 🐛 **fix**: Updated `promissory_payment_deposits_screen` to bind to `data_payload.deposits` instead of full payload to correctly render list
- 📦 **deps**: Added `promissory` directory to `pubspec.yaml` assets so mock files can be loaded by the interceptor
- 🐛 **fix**: Created missing `promissory/json` directory to resolve `pubspec.yaml` asset error
- 🔧 **fix**: Reverted `promissory_payment_deposits_screen` binding to `{{data_payload}}` and removed mock interceptor to allow real API calls (aligned with `promissory_deposits_screen`)
- 🐛 **fix**: Updated `promissory_payment_deposits_screen` to bind to `{{data_payload.deposits}}` as logs confirm the real API returns a Map (Customer Info) for this screen, unlike the List returned for the other screen.
