## 25
- 🐛 **fix**: Fixed expression evaluation in `CustomSetValueActionParser` and `StatefulWidgetParser` to support ternary (`a ? b : c`) and negation (`!a`) - checkbox now toggles visually and enables button.

## 06
- ✨ **feat**: Created complete promissory flow with 8 STAC Dart screens (intro, rules, issuer, receiver, data, confirm, payment, success)
- ✨ **feat**: Converted Flutter UI from promissory_docs to STAC syntax for each screen
- 🔧 **chore**: Registered all promissory screens in StacWidgetLoader for navigation
- 📝 **docs**: Added promissory strings to GET_strings.json with Persian localization
- 🔧 **chore**: Added سفته (promissory) flow to GET_menu-items.json linearFlows
- 🐛 **fix**: Fixed StacNavigateAction usage - replaced widgetType parameter with StacRawJsonAction for proper navigation
- 🐛 **fix**: Added StacAliasTextStyle and StacRawJsonAction helper classes to each promissory screen
- 🐛 **fix**: Fixed missing icons (replaced ic_receipt/ic_arrow_right with available assets) and fixed layout in promissory_intro/payment/rules
- 🐛 **fix**: Refactored promissory_rules checkbox to use StacContainer implementation as temporary workaround for unsupported widget
- 💄 **style**: Refined promissory_intro UI: align tabs RTL, swap card content (text right, icon left), update specific icons and texts
- 🐛 **fix**: Final layout adjustments for promissory intro (header text correction, removed arrow, text alignment)
- 🐛 **fix**: Reverted to custom checkbox implementation in promissory_rules as standard 'checkbox' widget is unsupported
- 💄 **style**: Refined promissory_rules UI: added header divider, updated rules text to match design, fixed generic checkbox label
- 🐛 **fix**: Fixed promissory_rules button not enabling by removing hardcoded 'enabled: false' property
- 🔧 **chore**: Updated pubspec.yaml to correctly include promissory flow assets directories

## 04
- 🐛 **fix**: Updated `login_flow_linear_verify_otp.json` to use `flowNext` for proper flow navigation on success.
- 🐛 **fix**: Resolved JSON syntax errors in `login_flow_linear_verify_otp.json`.
- 🔄 **fix**: Synced `flowNext` change to `GET_login_flow_linear_verify_otp.json`.
- 💄 **style**: Replaced `Padding` widget with `Container` (with padding) in `login_flow_linear_verify_otp.json` and its API counterpart to ensure robust padding application for `SingleChildScrollView` content and bottom button.
- 💄 **style**: Aligned `TextFormField` content padding in `login_flow_linear_verify_otp.json` with Dart implementation.
- 🔄 **fix**: Synced "Send Again" button size (140x56) from local JSON to `GET_login_flow_linear_verify_otp.json`.
- 🐛 **fix**: Removed `SizedBox` wrapper around `TextFormField` in Dat/API files to prevent input shrinking on error.
- 💄 **style**: Updated `TextFormField` content padding to `16.0` vertical in Dart/JSON/API to ensure 56px height.
- 💄 **style**: Updated "Send Again" button size to `140x56` in all files (Dart, JSON, API, single page) to match input height.
- 🐛 **fix**: Applied button size (140x56) and layout fixes to `GET_tobank_verify_otp.json` (API Mock) as it is the active source in debug mode.
- 💄 **style**: Synced content padding (16.0) to `tobank_verify_otp.json` (Local) for consistency.

